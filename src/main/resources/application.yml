spring:
  datasource:
    url: jdbc:mysql://192.168.159.1/jpa
    username: root
    password: mysql
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      # 更新或创建数据库。
      ddl-auto: update
      # 在控制台显示sql语句。
    show-sql: true